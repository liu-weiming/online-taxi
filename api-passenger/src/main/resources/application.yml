server:
  port: 8081

spring:
  application:
    name: api-passenger

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

  redis:
    host: localhost
    port: 6379
    database: 0